.row
  .eight.columns.offset-by-two
    .th
      .vertical-align2
        .panel.callout.radius
          .td
            .right
              %h4 Total number of invites:
              .header
                %h1.offset-by-five
                  = @user.invitation_count
            = image_tag @user.avatar_path
            .right
              %h6
                Email:
                = @user.email
              %h6 
                = @user.gender
            %h4
              = @user.full_name
            %h6
              = @user.username
